{
  "mcpServers": {
    "filesystem": {
      "command": "${NODE_EXECUTABLE_PATH}",
      "args": [
        "${NPX_CLI_PATH}",
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${MCP_WORKSPACE_PATH}"
      ]
    }
  }
}